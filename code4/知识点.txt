shm原理与动态库一样通过映射到虚拟地址空间中的共享区被进程看到，物理内存就叫共享内存
创建共享内存只需要创建一次，其他进程直接通过约定好的key值挂接到自己的虚拟地址空间中
对共享内存进行读写操作时没有使用system call就当作malloc的空间一样使用强转使用
shm是所有ipc最快的因为不需要使用系统调用，直接映射到自己的虚拟地址空间读写操作都会被直接看到
与动态库映射不同的是动态库是mmap映射，共享内存是shmat
缺点：没有数据同步机制会导致数据不一致即读写同时运行读取信息不完整
shm在创建时必须是4096的整数倍 删除时要先解绑再删除将引用计数归零·